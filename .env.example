# Required - Azure AI Foundry project endpoint
# New names (preferred):
AZURE_AI_PROJECT_ENDPOINT=https://<your-resource>.services.ai.azure.com/api/projects/<your-project>
AZURE_AI_MODEL_DEPLOYMENT_NAME=gpt-4o
# Legacy names (still supported as fallback):
# PROJECT_ENDPOINT=https://<your-resource>.services.ai.azure.com/api/projects/<your-project>
# MODEL_DEPLOYMENT_NAME=gpt-4o

# Optional - GitHub MCP server (enables GitHub search/repo tools for Researcher)
GITHUB_PERSONAL_ACCESS_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx

# Optional - Azure Monitor tracing (leave empty for console output)
APPLICATION_INSIGHTS_CONNECTION_STRING=

# Optional - Agent Memory (requires chat + embedding model deployments)
AZURE_AI_CHAT_MODEL_DEPLOYMENT_NAME=gpt-4o
AZURE_AI_EMBEDDING_MODEL_DEPLOYMENT_NAME=text-embedding-3-small
AZURE_AI_MEMORY_STORE_NAME=geektime_pipeline_memory

# Optional - Publish workflow to Foundry (required for `python -m pipeline.publish`)
AZURE_SUBSCRIPTION_ID=
AZURE_RESOURCE_GROUP=
AZURE_ACCOUNT_NAME=
AZURE_PROJECT_NAME=

# Optional - Service principal auth (required for Docker; leave empty if using az login locally)
AZURE_TENANT_ID=
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
